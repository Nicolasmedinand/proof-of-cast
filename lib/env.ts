import { z } from 'zod'; const envSchema=z.object({NEYNAR_API_KEY:z.string(),DATABASE_URL:z.string(),BASE_RPC_URL:z.string(),WALLET_PRIVATE_KEY:z.string(),CONTRACT_ADDRESS:z.string().optional().default('0x0000000000000000000000000000000000000000'),NEXTAUTH_SECRET:z.string(),NEXTAUTH_URL:z.string(),NODE_ENV:z.enum(['development','test','production']).default('development'),}); export const env=envSchema.parse({...process.env});