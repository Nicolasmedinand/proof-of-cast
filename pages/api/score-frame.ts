import type { NextApiRequest, NextApiResponse } from 'next'; export default async function handler(req:NextApiRequest,res:NextApiResponse){ const fid=Number(req.body?.fid||0); return res.status(200).json({ version:'1', title:'Your Proof of Cast', image:`https://YOUR_DOMAIN/api/og/score?fid=${fid}`, buttons:[{label:'Back', action:'post', target:'https://YOUR_DOMAIN/api/frame'}] }); }