import type { NextApiRequest, NextApiResponse } from 'next'; import { mintBodySchema } from '@/lib/schemas'; import { mintBadge } from '@/lib/mint'; import { basescanTx } from '@/lib/links'; export default async function handler(req:NextApiRequest,res:NextApiResponse){ if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'}); const parsed=mintBodySchema.safeParse(req.body); if(!parsed.success) return res.status(400).json({error:'Invalid body', issues: parsed.error.issues}); const { address }=parsed.data; const txHash=await mintBadge(address,0); return res.status(200).json({ txHash, explorer: basescanTx(txHash) }); }