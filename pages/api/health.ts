import type { NextApiRequest, NextApiResponse } from 'next'; import { prisma } from '@/lib/prisma'; export default async function handler(_req:NextApiRequest,res:NextApiResponse){ try{ await prisma.$queryRaw`SELECT 1`; res.setHeader('Cache-Control','no-store'); return res.status(200).json({ ok:true, now:new Date().toISOString() }); }catch{ return res.status(500).json({ ok:false }); } }