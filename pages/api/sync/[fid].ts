import type { NextApiRequest, NextApiResponse } from 'next'; import { prisma } from '@/lib/prisma'; export default async function handler(req:NextApiRequest,res:NextApiResponse){ const { fid }=req.query; if(req.method!=='POST') return res.status(405).json({error:'Method not allowed'}); const id=Number(fid||0); await prisma.user.upsert({ where:{fid:id}, update:{ lastSyncAt:new Date() }, create:{ fid:id, username:`user${id}`, score:0, level:0, lastSyncAt:new Date() } }); return res.status(200).json({ fid:id, synced:true }); }