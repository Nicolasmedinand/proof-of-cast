import type { NextApiRequest, NextApiResponse } from 'next'; import { prisma } from '@/lib/prisma'; export default async function handler(_req:NextApiRequest,res:NextApiResponse){ const top=await prisma.user.findMany({ orderBy:{score:'desc'}, take:50, select:{fid:true,username:true,score:true,level:true}}); return res.status(200).json({ items: top }); }