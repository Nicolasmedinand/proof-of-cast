import type { NextApiRequest, NextApiResponse } from 'next'; import { prisma } from '@/lib/prisma'; export default async function handler(req:NextApiRequest,res:NextApiResponse){ const { fid }=req.query; const id=Number(fid||0); const user=await prisma.user.findUnique({ where:{ fid:id }}); return res.status(200).json({ fid:id, score:user?.score??0, level:user?.level??0, lastSyncAt:user?.lastSyncAt??null }); }